import { PlaceholderPage } from "@/components/ui/placeholder-page";

export default function BatchesPage() {
  return (
    <PlaceholderPage
      eyebrow="Workspace"
      title="Batches"
      description="Review planned and completed production batches in one place."
    />
  );
}
